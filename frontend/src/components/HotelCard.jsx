import{Link}from"react-router-dom";export default function HotelCard({h}){return <article className="card hotel-card"><img src={h.image} alt={h.name}/><div className="card-body"><div className="card-row"><h3><Link to={`/hotels/${h.id}`}>{h.name}</Link></h3><span className="rating">★ {h.rating}</span></div><p>{h.destination}</p><div className="card-row"><strong>₹{h.price.toLocaleString()} <small>/ night</small></strong><span><Link className="text-link" to={`/hotels/${h.id}`}>Details</Link> <Link className="btn btn-small" to="/booking">Book Now</Link></span></div></div></article>}