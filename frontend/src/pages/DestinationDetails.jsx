import{Link,useParams}from"react-router-dom";import{destinations,hotels,packages}from"../data";import HotelCard from"../components/HotelCard";import PackageCard from"../components/PackageCard";export default function DestinationDetails(){const{id}=useParams();const d=destinations.find(x=>x.id===Number(id))||destinations[0];const hs=hotels.filter(h=>h.destination===d.name);const ps=packages.filter(p=>p.destination===d.name);return <><section className="detail-hero" style={{backgroundImage:`url(${d.image})`}}><div><span className="pill">{d.country}</span><h1>{d.name}</h1><p>{d.description}</p></div></section><section className="section"><div className="container"><div className="section-head"><h2>Where to stay</h2><Link className="text-link" to="/hotels">All hotels →</Link></div><div className="grid-4">{(hs.length?hs:hotels.slice(0,2)).map(h=><HotelCard h={h} key={h.id}/>)}</div><div className="section-head mt"><h2>Travel packages</h2><Link className="text-link" to="/packages">All packages →</Link></div><div className="grid-4">{(ps.length?ps:packages.slice(0,2)).map(p=><PackageCard p={p} key={p.id}/>)}</div></div></section></>}