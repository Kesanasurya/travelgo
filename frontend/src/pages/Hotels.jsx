import{hotels}from"../data";import HotelCard from"../components/HotelCard";export default function Hotels(){return <section className="page-section"><div className="container"><span className="eyebrow">HANDPICKED STAYS</span><h1>Hotels</h1><p className="lead">Comfortable stays for every kind of traveler.</p><div className="grid-4">{hotels.map(h=><HotelCard h={h} key={h.id}/>)}</div></div></section>}