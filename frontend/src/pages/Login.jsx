import{Link,useNavigate}from"react-router-dom";import{useState}from"react";import{login}from"../api/authApi";export default function Login(){const nav=useNavigate();const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");const[loading,setLoading]=useState(false);const submit=async e=>{e.preventDefault();setError("");setLoading(true);try{const{data}=await login({email,password});localStorage.setItem("travelgo_token",data.token);localStorage.setItem("travelgo_user",JSON.stringify(data.user));nav("/")}catch(err){setError(err.response?.data?.message||"Unable to sign in. Check your details.")}finally{setLoading(false)}};return <section className="auth-page"><form className="auth-card" onSubmit={submit}><div className="logo">✈️ TravelGo</div><h1>Welcome back</h1><p>Sign in to manage your trips.</p>{error&&<div className="error">{error}</div>}<label>Email<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label>Password<input required minLength="8" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password"/></label><button className="btn full" disabled={loading}>{loading?"Signing in...":"Sign In"}</button><div>New here? <Link to="/register">Create an account</Link></div></form></section>}