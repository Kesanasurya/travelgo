import{useState}from"react";import{destinations}from"../data";import DestinationCard from"../components/DestinationCard";export default function Destinations(){const[q,setQ]=useState("");const list=destinations.filter(d=>(d.name+" "+d.country).toLowerCase().includes(q.toLowerCase()));return <section className="page-section"><div className="container"><span className="eyebrow">EXPLORE</span><h1>Destinations</h1><p className="lead">Find your next unforgettable place.</p><input className="wide-input" placeholder="Search destinations..." value={q} onChange={e=>setQ(e.target.value)}/><div className="grid-3">{list.map(d=><DestinationCard d={d} key={d.id}/>)}</div></div></section>}