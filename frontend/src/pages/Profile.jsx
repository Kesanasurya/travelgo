export default function Profile(){const storedUser=localStorage.getItem("travelgo_user");let user={name:"Traveler"};try{user=storedUser?JSON.parse(storedUser):user}catch{}const name=user.name||"Traveler";return <section className="page-section"><div className="container narrow"><span className="eyebrow">ACCOUNT</span><h1>Profile</h1><div className="profile-card"><div className="avatar">{name[0]?.toUpperCase()}</div><h2>{name}</h2><p>TravelGo member</p><button className="btn" onClick={()=>{localStorage.clear();location.reload()}}>Log out</button></div></div></section>}